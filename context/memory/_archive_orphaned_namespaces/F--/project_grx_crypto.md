---
name: project-grx-crypto
description: "GamepadOS 'GRX' big update — encrypted low-latency input layer; status, files, and what's next"
metadata: 
  node_type: memory
  type: project
  originSessionId: e27d7c60-9e1a-469e-9a3a-3e5cc31d3e13
---

**GRX (GamepadOS Realtime eXchange)** = the in-progress "big update": real encryption + lowest-feasible latency for the phone→PC input link. Started 2026 from a multi-agent research+adversarial-verify workflow.

**STATUS UPDATE 2026-06-30 (multi-agent verify audit + FIXES APPLIED & BUILT, all on-disk):** GRX is now LIVE on the client — `GRX_ENABLED=true` (MainActivity.kt:56). Crypto mirror verified byte-identical (Kotlin↔Python, 0 blockers); server GRX responder fully wired (handshake→decrypt→pad; legacy+GRX coexist, GRX_REQUIRED=False). Three real issues were found and ALL FIXED + build-verified this session:
- **(FIXED) shipped `pc-server/dist/GamepadServer.exe` was PRE-GRX** (old build 2026-06-29 09:29 had zero grx/cryptography). REBUILT 2026-06-30 19:14 via `python -m PyInstaller GamepadServer.spec` → now 42.8MB, Analysis-00.toc has grx_crypto/grx_session (4) + cryptography (95). Shipped exe is now GRX-aware.
- **(FIXED) native TX thread never `DetachCurrentThread`** → ART abort on disconnect once GRX establishes. Added detach guard before `return nullptr` at end of txThreadLoop (gamepad-engine.cpp ~L572, uses g_vm->GetEnv!=JNI_EDETACHED then DetachCurrentThread). Covers both UDP+AOA threads (shared loop). Applied BEFORE the exe rebuild (correct order).
- **(FIXED) transport-coordinator double-pad regression** — was gating `stopEngine()` on `linkAlive` (ACK-derived). Now MainActivity.getNetworkTelemetryJson emits `engineRunning` (=isEngineRunning) and App.tsx coordinator gates on engineRunning (usbdebug branch + auto branch) so the USB-debug WS can't coexist with a live native UDP socket during an ACK gap.
- Refuted non-issues (NOT bugs): R8 stripping grxSeal/onGrxControl (keep rule proguard-rules.pro:5); stale-Activity after rotation (configChanges handles it, AndroidManifest:36).
- Cosmetics FIXED: C++ stick neutral seed 127→128 (gamepad-engine.cpp UDP+AOA init); stale "dormant" comments refreshed (MainActivity.kt:52, gamepad-engine.cpp:83); App.tsx authToken 0xABCD1234→0 (native owns the field); removed ignored `package=` attr from AndroidManifest.
- **Device-breadth: `android-client` build.gradle.kts abiFilters now arm64-v8a + armeabi-v7a + x86_64** (was arm64-only); multi-ABI debug APK verified to contain all three `libgamepad_engine.so`. Universal sideload APK.
- **CONTROLLER UX + GYRO PASS (2026-06-30, build-verified, debug only):** audited UI for input loopholes + gyro smoothness via agents, then fixed. HIGH: panic-release on visibilitychange/blur/pagehide (backgrounding no longer latches a held button forever via the 16ms keep-alive); per-pointer stick ownership in useStick (2nd finger can't hijack/recenter an active stick); curtain transition timer-tracking + re-entrancy guard + 1.2s failsafe (navigateTo/openEditor/closeEditor via runCurtainSequence — no more wedged black screen on double-tap/back). MEDIUM: Dpad rewritten for 8-way roll-through + diagonals (parent capture + angle bands, sectors now pointerEvents:none); TriggerPill now analog (fillAt from finger Y); connectToPC try/catch; gyro HUD bar left/right safe-area inset. Gyro: sensor delivery moved to a dedicated HandlerThread (off UI thread — kills jitter under load); added neutralRollDeg recenter + `setGyroNeutral(reset)` JS-bridge method + "Set neutral/Reset" buttons in Tilt settings; expo steering curve (0.3) in App.tsx useGyro; OneEuroFilter tuned minCutoff 1.2→1.6/beta 0.15→0.2. LOW: onLostPointerCapture safety-release on all widgets, Dialogs ad-interval cleanup, usbStatus reset-to-idle, safe-area getters try/catch. DEFERRED (risk w/o device test): PresetEditor 1264→1280 coord unify (could shift saved layouts), dead `rumble` state removal, usbWS worker 333Hz idle interval.
- **GYRO STEERING ALGORITHM REWRITE (2026-07-01, build-verified debug):** root-caused the "locks at ~30° when phone held vertical / sensitivity varies with hold angle" bug = Euler `getOrientation()` roll gimbal-locks near pitch 90°. REPLACED with gravity-projection-relative-to-neutral in MainActivity.kt onSensorChanged: world-up in device frame = R[6],R[7],R[8]; steering = signed angle atan2(cross,dot) of the screen-plane projection (R[6],R[7]) vs a captured neutral (neutX/neutY, NaN=auto-capture next sample). Singularity-free at ANY hold; best exactly in the phone-facing-you-vertical pose. Removed the landscape remap/getOrientation (fusionRemapped/fusionOrient/currentDisplayRotation now unused). setGyroNeutral now captures gravUX/gravUY. Constants STEER_MIN_PROJ=0.2, **STEER_SIGN=-1.0 (FLIP if tilt-right steers left on device — UNVERIFIED sign)**. getGyroscopeDataJson nx=fusedRollDeg (neutral baked in).
- **GYRO TWO-MODE REDESIGN (2026-07-01, supersedes the gravity-projection attempt above):** the gravity-projection-vs-neutral scheme was UNSTABLE (short-vector atan2 near neutral + one-sample auto-neutral) and wrong-direction — reverted. NEW: onSensorChanged remaps to landscape frame then roll=STEER_SIGN*atan2(up.x,up.y) [=atan2(remapped R[6],R[7])], pitch=PITCH_SIGN*asin(up.z) [=asin(remapped R[8])]; both 1€-filtered (rollFilter+pitchFilter), NO neutral, stable in vertical hold, decoupled. STEER_SIGN=-1.0/PITCH_SIGN=1.0 (var, UNVERIFIED — flip after logcat). TEMP `Log.d("GYRO",...)` every 200ms for `adb logcat -s GYRO` sign-verification (REMOVE later). setGyroNeutral DELETED (native+JS). TWO MODES via JS `gyroMode` state ("racing"|"3d", localStorage "gyro_mode"): racing = nx→left-stick-X + shows tilt bar; 3D = nx→right-stick-X, ny→right-stick-Y (aim/look; yaw intentionally NOT used — drifts w/o magnetometer) + hides bar, pill shows "GYRO 3D". Mapping at App.tsx steering block (`gyroMode==="racing"? lx-=gx*60 : {rx-=gx*60; ry+=gy*60}`). Mode toggle card replaced the recenter card (Racing/3D buttons). gyroMode threaded through ControllerScreen+SettingsScreen+TabSystem. getGyroscopeDataJson unchanged (nx=roll, ny=pitch). GOTCHA: App.tsx has MIXED line endings — TabSystemProps/tilt-settings region is CRLF (multi-line Edit fails "not found"); use single-line Edit anchors or PowerShell [System.IO.File]::ReadAllText + CRLF-normalized .Replace + WriteAllText UTF8-no-BOM.
- **RELEASE PUBLISHED (2026-07-01, per RELEASE.md — NOT yet activated in admin portal):** Built + published BOTH artifacts to website/backend/downloads/ with the two-mode gyro (racing/3D + calibrate) + GRX + rumble-log fixes: Android `GamepadOS-1.2.0.apk` (versionCode 12, release-signed w/ release.keystore) and PC `GamepadServer-Setup-1.1.6.exe` (installer wraps dist/GamepadServer.exe; bumped APP_VERSION in server.py + AppVersion/VersionInfoVersion in installer/GamepadServer.iss, all 3 in sync per RELEASE.md's "must match" rule). SHA-256 of the new installer added to website/frontend/index.html `.dl-trust` block (was previously undocumented/missing despite RELEASE.md describing it) — rebuilt into dist/. Old versions (1.1.9 apk, 1.1.5 exe) preserved under old filenames, not deleted. **REMAINING (needs the site owner — RELEASE_KEY + live-user-facing, did not do autonomously):** Admin portal → 📦 Releases → Register & Activate each new file (this is what actually flips /api/version and starts offering the update to users) — activating deactivates the previous version. Then verify via GET /api/version and one real in-app-updater test per release. Website deploy is also still the user's to trigger (not pushed).
- **RUMBLE (2026-07-01):** full chain audited & correct on ALL sides (server ViGEm register_notification `_cb`→sess.rumble_large/small→`b"RMB"` to addr on both GRX+cleartext handle_frame; C++ RX 'RMB' 5B→rumbleSeq++; getNativeRumble→getRumbleState "seq:l:r"; App.tsx 33ms poll→onRumblePacket→bridge.triggerRumble→doVibrate; rumbleOn defaults true). Not a code bug → runtime (game/ViGEm FFB or device). Added server diagnostic `print("[RMB] game FFB ...")` on FFB start/stop in _cb (server.py) to localize; exe rebuilt.
- **INSTALL (2026-07-01):** phone DAIFEYGEKB89V4QG blocks `adb install` with INSTALL_FAILED_USER_RESTRICTED (needs Developer Options→"Install via USB"). Old com.gamepad.client was UNINSTALLED during the attempt (fresh install → re-pair needed); com.remotegamepad (separate old app) still present. APK pushed to /sdcard/Download/GamepadOS-debug.apk for manual tap-install. adb path: use PowerShell (`& adb push ... /sdcard/...`) — Git Bash MSYS mangles /sdcard → C:/Program Files/Git/sdcard.
- **VERIFIED:** controller-ui vite build + `tsc -p tsconfig.check.json` both clean; android-client assembleDebug BUILD SUCCESSFUL (3 ABIs); GRX/server self-tests 12/12. Builds are DEBUG-verified only. NOTE: the v1.2.0 release APK built earlier this session PRE-DATES this UX/gyro pass — a fresh release build is needed to ship these UI/gyro fixes. **NOT YET DONE: (a) release build + versionCode/versionName bump (currently 11/1.1.9) for distribution; (b) on-device test (detach fix, double-pad fix, GRX end-to-end encryption with the new exe); (c) re-copy new APK to website/backend/downloads/ + update server-side version per RELEASE.md.**

- **RELEASE BUILT 2026-07-03 (pairing fix + calibrate-button move — NOT yet activated in admin portal):** Fixed the sticky `gp_wired_pref` wireless-drop bug: a stuck "usbdebug" pref made the 1.5s transport coordinator stopEngine() a fresh wireless link (~2s disconnect). QR path already reset the pref (Dialogs.tsx); added `setWiredPref("auto")` to `handleManualConnect` (App.tsx) — both wireless entry points now clear it. Also moved the 3D-gyro CALIBRATE button from canvas center to top-center edge (`top-2`). Built + staged to website/backend/downloads/: `GamepadOS-1.2.7.apk` (versionCode 19, release-signed, cert SHA-256 5b5537c6…, contains fresh controller-ui bundle) and `GamepadServer-Setup-1.1.15.exe` (fresh GRX-aware PyInstaller exe + Inno; installer SHA-256 A8FD09CA3778B708555B5EC103F46AB68EE5D26B678D3F4E8DCBAF523C73F732, updated in website/frontend/index.html .dl-trust). All version touchpoints bumped in sync (server.py APP_VERSION, .iss AppVersion+VersionInfoVersion, build.gradle.kts 19/1.2.7). REMAINING for user: admin portal Register & Activate both files (RELEASE_KEY), redeploy frontend, verify /api/version + one real updater run.

- **USB-TETHER ROOT CAUSE FOUND + FIXED 2026-07-03 (live-debugged with real phone+PC, verified stable 75s+):** "Tethering not working since ~1.1.9" = TWO server bugs, both fixed in server.py/grx_session.py (shipped in rebuilt 1.1.15): (1) **lan_ip misdetection**: when the phone tethers with mobile data, Windows default-routes internet through the phone → `_route_ip()` = the tether adapter's own IP; AND `getaddrinfo(hostname)` (in `_all_ipv4`) missed the Wi-Fi IP entirely (only VirtualBox IP listed) → `get_lan_ip()` picked the TETHER IP → the post-1.1.9 "token 0 accepted only off primary LAN" auth rule saw the phone as ON-LAN → every tether frame silently dropped (no ACK → phone loops start/6s-verify/stop forever). Fix: new `_win_ipv4_adapters()` (ctypes GetAdaptersAddresses, locale-independent; GAA flags 0xE — 0x1 is SKIP_UNICAST!; read sockaddr via string_at, c_char-array slicing truncates at NULs) merged into `_all_ipv4`, plus explicit `is_usb_tether_client()` (client /24 matches a local adapter whose Description ~ /NDIS|NCM|Tether|Internet Sharing/i, 5s TTL cache) OR'd into the token-0 rule. Also fixes QR showing wrong IP when tether carries internet. (2) **UDP-thread kill via GRX misroute**: `grx_session.is_handshake()` matched on FIRST BYTE ONLY; legacy 20B input frames start with a LE timestamp whose low byte sweeps 0-255 → ~1/128 input frames misrouted into the handshake path; a fake CONFIRM (20B unpacked as 37B) raised struct.error which was UNCAUGHT (only HandshakeError was) → **UDP loop thread died, port stayed bound but silent → ALL transports dead until server restart** (this also explains random Wi-Fi/pairing deaths!). Fix: `_HS_MIN_LEN` length check in is_handshake (HELLO≥34/SHELLO≥65/CONFIRM≥33), broad `except Exception` on the hello/confirm handler, and try/except around every handle_frame call so the UDP thread can never die. GRX self-tests still 12/12. Client/APK needed NO changes for this. Debug method that worked: raw python UDP listener on 7777 proved packets arrive (585/20s), server console from source (`python server.py`) showed the GRX misroute + lan_ip lines. NOTE: phone-side engine can wedge ("initNetworkNative called while engine already running") after PC-side flapping — force-stop app to clear. Xiaomi Redmi (2311DRK48I, Android 16) still uses RNDIS ("Remote NDIS based Internet Sharing Device" adapter, works on Win11 26200). Home/Guide button is intentionally NOT forwarded (Game Bar resets tether adapter) — user informed, kept as-is.

- **"BUTTONS DEAD ON WIRED, FINE ON WIRELESS" ROOT CAUSE + FIX 2026-07-03 (same session, live XInput-level debug):** input WAS arriving (XInput poll via ctypes xinput1_4 showed 105 state changes) but on **slot 1** while a ghost pad sat on slot 0 — games only listen to player 1. Cause: PadManager keys sessions by SOURCE IP; a wireless→wired switch (wlan IP → rndis IP) creates a SECOND ViGEm pad while the old session lingers (3s idle retire — or indefinitely if a wedged phone engine keeps streaming), and the wired pad stays parked on XInput slot 2/1 even after the old pad frees. Fix in PadManager.acquire: **single-phone transport migration** — if a new IP-keyed session arrives while exactly ONE other IP-keyed session exists and it's been quiet >0.5s, REBIND that session (same PadSession → same ViGEm pad → same XInput slot, reset last_ts/last_report) instead of creating a pad; prints "Controller session A -> B (pad + XInput slot kept)". WS ("usb:N")/"aoa" sessions excluded as migration sources (a WS idles legitimately on the dashboard; stealing it would let two transports drive one pad). Verified: fresh server + tether reconnect = single session on slot 0. Also confirmed working: `del gamepad` probe-pad release (vgamepad __del__ → vigem_target_remove; isolation-tested), fixed `get_lan_ip` now prints route=tether but picks Wi-Fi 192.168.1.35. Debug trick: poll XInput directly with ctypes (XInputGetState slots 0-3) while user presses buttons — instantly shows which slot receives input.

- **MULTI-DEVICE AUDIT + GUIDE-BUTTON VERDICT 2026-07-03 (final 1.1.15 build, SHA E93EE691…45DF7A6):** user's "middle top key creates the issue" = Guide (bit 14): the OLD installed server forwarded Guide → Xbox Game Bar → game loses focus ("buttons dead") + tether adapter reset; current server drops Guide (by design, user approved "no guide") — fixed by upgrading to 1.1.15, no code change needed today. Multi-device (2-4 phones) audit fixes: (a) UDP handle_frame ACK'd BEFORE padmgr.acquire → a 5th device (or pad-create failure) showed "connected" but drove nothing — ACK moved after acquire; (b) same fake-connected in the WS path (_recv ACK'd with sess None) — now skips ACK; (c) pad-migration steal race: two same-Wi-Fi phones + a 0.5s hiccup on A while B first connects → B stole A's pad — migration now requires a /24 SUBNET CHANGE (the wifi↔tether signature) and a bounded 0.5-3.5s quiet window. Verified clean: per-IP pads, MAX_PADS=4=XInput, per-session rumble/ordering, per-IP GRX. Known cosmetic: telemetry transport label is last-writer-wins with 2+ devices. 1.1.15 total content: tether auth (adapter-enum via ctypes GetAdaptersAddresses + tether-aware token-0), GRX misroute UDP-thread kill, pad/slot migration, no-ACK-without-pad. **GUIDE RE-ENABLED (user demanded the Xbox overlay — "no guide" earlier was a misread):** apply_inputs now forwards bit 14 → XUSB_GAMEPAD_GUIDE gated by `sess.allow_guide` (PadSession slot, default True; UDP handle_frame sets it per frame = `not is_usb_tether_client(ip)`) — Guide initially suppressed on tether, then LIVE-RETESTED (90s of Guide presses over tether on Win11 26200: guide bit reached pad via XInputGetStateEx ordinal-100, 0 pad drops, 0 adapter flaps) → the old "Game Bar resets the RNDIS adapter" failure does NOT reproduce → **Guide now forwarded on ALL transports** (sess.allow_guide kept default-True as escape hatch). User confirmed wanting the Xbox overlay ("no guide" earlier was a misread). Hash updated again after this change (see index.html .dl-trust for current). Windows GameBar UseNexusForGameBarEnabled at default (=on) so overlay appears. Installed exe on THIS PC still 1.1.14 until user runs installer\Output\GamepadServer-Setup.exe.

- **UI POLISH + APK 1.2.8 (2026-07-04, preview-verified):** three fixes in controller-ui, all confirmed in browser preview at phone aspect: (1) 3D-gyro CALIBRATE + GYRO toggle were OVERLAPPING at top-center → moved CALIBRATE inline into the same flex row as the GYRO button (App.tsx ~L1220), now side-by-side 8px gap; (2) racing-mode gyro indicator had a `borderBottom` line under it → removed (kept the boxShadow glow); (3) LT/RT triggers rested RED (RED_NORM=rgba(180,10,10,.85) — misnamed const, RED_HELD is actually cyan) while all other buttons rest translucent-cyan → added `CYAN_NORM=rgba(0,212,255,0.12)` in GamepadWidgets.tsx, TriggerBlock resting pill now uses it (matches LB/RB/dpad; ABXY keeps its own per-face colors by design). Built APK **1.2.8 / versionCode 20** (release-signed, cert 5b5537c6…), staged website/backend/downloads/GamepadOS-1.2.8.apk. Downloads folder holds both latest apps: GamepadOS-1.2.8.apk + GamepadServer-Setup-1.1.15.exe (older versions preserved). PC server unchanged this round (still 1.1.15, hash BED2DF99…CF7BE3). STILL PENDING (user): run installer locally, admin-portal Register & Activate PC 1.1.15 + App 1.2.8, redeploy frontend.

- **MULTI-DEVICE PAIRING AUDIT + GHOST-PAD FIX 2026-07-04 (loopback-simulated, all 4 combos PASS at XInput level):** user asked to verify 2-4 devices, same/different connection, for overlaps. Built a test harness (scratchpad/multi_device_test.py) that simulates devices as UDP sockets bound to loopback aliases (127.0.0.x — Windows allows binding any 127/8 addr, gives arbitrary same/diff-subnet source IPs) sending real 20B frames with the real pairing token (read from %LOCALAPPDATA%\GamepadServer\pairing_key.txt), verified via XInputGetState slot polling. FOUND + FIXED the real "overlapping" bug: **ghost virtual pads**. The per-pad rumble callback `_cb` (registered via pad.register_notification) captured the PadSession, which holds the pad → reference cycle `pad->cmp_func->_cb->s->pad` → refcounting never frees it → dead pads lingered as GHOST (0-input) XInput controllers until Python cyclic-GC eventually ran. A ghost squats a slot (esp. slot 0) → pushes the next real phone to player 2 → "buttons don't work" (games read player 1 only). FIX in PadManager._free: `vigem_target_x360_unregister_notification(pad._busp,pad._devicep)` + `pad.cmp_func=None` + `s.pad=None` → pad unplugs IMMEDIATELY on session drop. ALSO tightened the transport-switch migration guard to `len(self.sessions)==1` (only migrates when it's the SOLE device — a 2nd device of any transport now always gets its own fresh pad, never a steal). Verified: 2 same-subnet→2 pads, 2 diff-subnet concurrent→2 pads no merge, 1-device wifi/tether switch→stays 1 pad same slot, 3 same-subnet→3 pads. Folded into PC 1.1.15 (not version-bumped — 1.1.15 was never activated yet). APK unchanged (server-only fix; current app = 1.2.9). Ghost-pad fix likely also cures earlier random "wired buttons dead" reports.

- **ROLLBACK POINT PRESERVED 2026-07-04:** current "latest" archived at `F:\hlooo\releases-archive\v1.2.9-android\` (GamepadOS-1.2.9.apk sha 62803EC4… + src: App/Widgets/GamepadWidgets/Dialogs.tsx, build.gradle.kts, MainActivity.kt, gamepad-engine.cpp) and `v1.1.15-pc\` (GamepadServer-Setup-1.1.15.exe sha CED9FFB8… + src: server.py, grx_session.py, grx_crypto.py, .iss/.spec, requirements.txt). Binaries re-deployable as-is; releases-archive/README.md documents all folded-in fixes. Same hashes as website/backend/downloads (byte-identical). LATEST shipped set = APK 1.2.9 (code 21) + PC 1.1.15; both in downloads with older versions preserved. Still pending user: run installer locally + admin-portal Register&Activate + frontend redeploy.

- **23-ENHANCEMENT GYRO+LATENCY PASS → APK 1.3.0 (code 22) + PC 1.1.16, BUILT & STAGED 2026-07-04:** a multi-agent analysis workflow proposed 30 gyro/latency enhancements, adversarially verified vs code + the 20-byte wire contract → 23 survived, all implemented (wire format unchanged). Builds verified: tsc+vite clean, gradle assembleRelease OK (all 3 ABIs, release-signed cert 5b5537c6…), PyInstaller OK (42.8MB GRX-aware), GRX self-tests 12/12; controller-ui changes browser-preview-verified. Standout fix: **GRX seal now gated on `doSend`** (gamepad-engine.cpp ~L303) — was sealing every ~500Hz idle poll and discarding it. Also: GRX Cipher reuse (GrxCrypto.kt), SO_PRIORITY=6/WMM-AC_VO, adaptive 30→60Hz keep-alive during rumble, removed shipped Log.d("GYRO")+dead fusionOrient, ACK-after-apply (server.py), smooth deadzone, scale60 hidden-radial-deadzone bypass, racing recenter (CALIBRATE both modes), 3D 2D-look indicator, tilt-throttle setting (localStorage gyro_throttle, default OFF, racing-only). DEFERRED (coded-risk, not applied blind): per-pad ViGEm lock, base64→binary JS bridge, GRX cached jbyteArray. DEVICE-UNVERIFIED guesses still shipped: STEER_SIGN/PITCH_SIGN, tilt-throttle Y sign (all behind CALIBRATE/default-OFF). Staged to website/backend/downloads (GamepadOS-1.3.0.apk sha 6380D6BB…35E6 (gyro line moved to vertical-center behind all buttons), GamepadServer-Setup-1.1.16.exe sha 2353F100…5A65); frontend .dl-trust SHA updated; rollback archived at releases-archive/v1.3.0-android + v1.1.16-pc. **Post-build regression audit (multi-agent, pairing/buttons/rumble/UI): 3/4 clean, pairing ACK-after-apply concern refuted; found+fixed 1 low-sev cosmetic bug (3D look-dot Y not recentered on gyro-off, App.tsx targetBy=0).** Then per user request moved the gyro moving indicator (racing bar + 3D dot) BEHIND the buttons (it overlapped BACK/LT/RT): (1) extracted it from the z-10 HUD into its own zIndex-0 layer (controls stay z-10, tappable); (2) **raised the button canvas z-0 → z-[5]** so buttons paint over the indicator. GOTCHA: equal z-0 + DOM order did NOT work (stacking quirk kept indicator on top) — the explicit higher canvas z-index is the real fix; verified via opaque-bar occlusion screenshot. APK rebuilt → hash above (6380D6BB…35E6 (gyro line moved to vertical-center behind all buttons)) is the FINAL build (all 23 + targetBy fix + indicator-behind-buttons). Full detail in SESSION_HANDOFF_2026-07-04.md. **LATEST shipped set = APK 1.3.0 + PC 1.1.16.** Still pending USER: run installer locally, admin-portal Register&Activate 1.3.0+1.1.16, frontend redeploy, on-device GRX/gyro validation.

- **GYRO INDICATOR REDESIGN TO USER SPEC + APK 1.3.0 REBUILT 2026-07-05 (preview-verified both modes):** user REJECTED the 2026-07-04 "vertical-center behind buttons" indicator placement (looked like it slid ABOVE buttons on device — the cyan glow + bright fill bled through the semi-transparent button fills; the opaque-bar occlusion test had missed this). New user-approved spec (see [[feedback-gyro-indicator-design]]): racing bar + 3D dot at the VERY TOP edge ABOVE the GYRO toggle (HUD pushed down `calc(safe-top + 10px racing / 48px 3d)`), bar **edge-to-edge (w-full) like 1.2.9 but 6.5px thick** (= 65% of 1.2.9's 10px; user's "65% width" meant THICKNESS, not screen span — first attempt as 65%-of-screen-span was corrected same session), **ALL cyan boxShadow glows removed** (bar, fills, 3D box, dot), still own zIndex-0 layer under canvas z-[5]/controls z-10, pointer-events-none. ALSO FIXED a latent bug: the browser `deviceorientation` fallback in useGyro still drove the bar via `style.width=%` (pre-scaleX mechanism) → stamped `width:0%` on the w-full fills = bar could NEVER fill in any browser preview/Wi-Fi-browser test; now writes `scaleX()` like the native path (App.tsx ~L628). Verified in vite preview: 65.0% width @top y=2, z-order 0<5, fills track simulated tilt, both modes, zero shadows. Rebuilt bundle (vite, robocopy'd to android assets) + APK same version **1.3.0/code 22** (never activated, so no bump; release-signed) → sha 64E23202… (superseded LATER SAME DAY by the opaque-widget rebuild — see next bullet; also supersedes intermediate E1F16AE4… 65%-span build and 6380D6BB…35E6) in website/backend/downloads (GamepadOS-1.3.0.apk + GamepadOS.apk) AND releases-archive/v1.3.0-android (apk + src/App.tsx synced); new bundle presence verified INSIDE the APK zip (assets/dist/index.html contains height:"6.5px", no width:"65%"). PC 1.1.16 untouched; frontend .dl-trust untouched (lists PC installer hash only). Gradle build note: `build_apk.bat` fails from background shells — run foreground: set JAVA_HOME=F:\hlooo\tools\jdk\jdk-17.0.19+10, ANDROID_HOME=F:\Android\Sdk, then `F:\hlooo\tools\gradle-8.5\bin\gradle.bat clean assembleRelease` in apps/android-client. Still pending USER: install APK, admin-portal Register&Activate, on-device validation.

- **OPAQUE-WIDGET PASS + TRUE FINAL 1.3.0 REBUILD 2026-07-05 17:14 (session "Gyro indicator z-index layering"/185bb948):** after the 64E23202 build, the top-edge bar still bled through SEMI-TRANSPARENT widget fills on device → added opaque silhouette fills to the play widgets (Widgets.tsx TriggerPill/WidgetBase/BACK chip + GamepadWidgets.tsx + App.tsx, edited 17:07–17:10; 6 silhouette fills + the 6.5px bar verified inside the packaged bundle). Rebuilt APK **1.3.0/code 22 → TRUE FINAL sha 4425155061594BA6E6C52ADE44165FC020401023665ED57706EF4CCF39202D8D** — staged byte-identical to downloads (GamepadOS-1.3.0.apk + GamepadOS.apk), releases-archive/v1.3.0-android (src synced), and build output. **Installed on the phone via adb (md5 MATCH) + launched.** PC 1.1.16 also INSTALLED on the PC (registry, 2026-07-04). Website repo committed ("release: update GamepadOS to 1.3.0 (reverted layout fix)", author gamepadadmin01) **and pushed — main==origin/main** → Vercel auto-deploy should have run. STILL REMAINING (verified 2026-07-05 22:00): (1) admin-portal Register&Activate App 1.3.0 + PC 1.1.16 (RELEASE_KEY — the one blocking step for /api/version + updater); (2) on-device play-test (GRX drives pad post-seal-gate, STEER_SIGN/PITCH_SIGN, tilt-throttle sign, RTT); (3) one updater test per platform. OPEN THREAD: user claimed admin-portal features missing; exhaustive check found none anywhere (last portal commits July 2); "which features?" question unanswered (session hit usage limit).

- **FEEDBACK ROOT CAUSE + 1.3.1 (code 25) ALL-FLAVOR BUILD 2026-07-11:** Play Store accepted 1.3.0 (code 24, built 08-07; 22 burned testing, 23 burned by the 16KB-alignment upload) but in-app feedback + update check were DEAD: UI loaded from `file:///android_asset/` and modern WebView blocks fetch() from file:// origins regardless of the (deprecated) allow*FromFileURLs flags. FIX (prototyped by Antigravity in hlooo-workspace, reviewed + re-applied to real tree + extended): WebViewAssetLoader serves the UI from https://appassets.androidplatform.net/assets/dist/index.html (androidx.webkit:webkit:1.10.0); flags removed; **one-time localStorage migration added** (assets/migrate.html loads once from the old file:// origin → dumps localStorage via AndroidBridge.onLegacyStorageDump → replayed into new origin + location.reload; SharedPreferences "gamepados"/webstorage_migrated flag + 4s watchdog) — without it every user would lose custom_pads/gyro settings. Also: CameraX 1.3.1→1.4.0 (clears the 16KB warning), gradle.properties -Xmx4g (R8 OOM'd default heap — 785MB hprof proof), **CustomPadEditor settings panel + Add-Widget menu got max-h-[calc(100vh-7.5rem)] overflow-y-auto** (LT/RT "Rectangular" mode pushed Throttle/Normal buttons off-screen). Backend CORS verified live: /api/support/ticket preflight from appassets origin passes. Built ALL 5 flavors 1.3.1/code 25 (gradle 8.9 + bundled JDK17, 5m41s) → store-releases/1.3.1/ (+RELEASE_NOTES.md); direct APK sha A16E1179…C07D also staged website/backend/downloads/GamepadOS-1.3.1.apk. Website repo: 3 local commits READY TO PUSH (channel-aware /api/version store-listing URLs [was uncommitted+undeployed from an earlier session], keepalive dead-host fix -9351→plain, 1.3.1 APK). PENDING USER: git push website (deploys Railway), upload GamepadOS-1.3.1-playstore.aab to Play, Register&Activate 1.3.1 in admin portal (/api/version still says code 22), upload aptoide/uptodown/amazon APKs, on-device feedback + migration test.

- **PLAY-NATIVE UPDATES → 1.3.2 (code 26) 2026-07-11 (same day):** user reported the Play build's update banner was driven by the WEBSITE /api/version but routed to Play where the release may not be published yet (review lag) = dead-end banner. Fix: playstore-only UpdaterBridge (app/src/playstore/java) on **Google Play In-App Updates** (com.google.android.play:app-update:2.1.0, playstoreImplementation; sourceSets split playstore off src/store/java) — checkPlayUpdate()/startPlayUpdate() + MainActivity.evalJs(); App.tsx UpdateChecker channel-aware (playstore never fetches the manifest; sideloaded playstore copies show no banner). Verified: stubbed-bridge browser test, tsc clean, Play classes in playstore AAB only (not direct APK). MEANWHILE USER SHIPPED 1.3.1: pushed website + Registered&Activated → live /api/version = 1.3.1/code 25 (sha a16e1179…), channel-aware backend live (?channel=playstore → Play listing). So 25 consumed → this is 1.3.2/code 26, all flavors in store-releases/1.3.2/, direct APK committed (1 commit awaiting user push; note user also committed "vedio removing" themselves). PENDING USER: push, upload 1.3.2 AAB to Play, Register&Activate 1.3.2, other stores, on-device Play-update test. **STORES PUBLISHED 2026-07-11 (user): Play + Uptodown + Amazon Appstore.** STORE_LISTING_URLS defaults filled same day (commit 3ebea99): amazonstore = package deep link /gp/mas/dl/android?p=com.gamepad.client (VERIFIED live → amazon.com/dp/B0H6MPPFZN); uptodown = gamepados.en.uptodown.com/android (user-provided, 404 until moderation publishes); playstore default was already right (404 until Google publishes); aptoide '' (not published).

- **8-AGENT BUG HUNT → 1.3.3 (code 27) + backend/admin hardening 2026-07-12:** multi-agent audit (verifiers kept dying on session limits, so Claude verified+fixed each finding inline against real code). Fixed ~18 real bugs. **App (1.3.3/code 27, all 5 flavors in store-releases/1.3.3, direct APK committed to website repo):** UpdateChecker mounted twice clobbered/deleted shared window.__onUpdate* globals → module-level subscriber set; preset/pos overrides now persisted to localStorage (were lost every restart); migration replay reloads unconditionally (finally) so JSI bridge always injects; shouldOverrideUrlLoading pins WebView nav to appassets origin (external URLs → browser); connect-failure Wi-Fi/multicast lock leak released; getNetworkDetailsJson via JSONObject; playstore bridge accepts DEVELOPER_TRIGGERED_UPDATE_IN_PROGRESS + UpdateChecker re-checks on visibilitychange (resume interrupted immediate update); stickmode added to SELF_MAPPED_TYPES; RangeSlider pointer-capture guard. **Backend server.js:** SSE stream re-validates session each 25s heartbeat + closes on logout/deactivation (was outliving the 20-min idle timeout); release activation atomic ($transaction) + validates versionCode/asset/file-exists (bad versionCode used to leave NO active release → ancient fallback + 404); pre-deploy 30-day sessions clamped to idle window; ticket schema max lengths + per-IP rate limit (6/hr); /api/version channel lookup Object.hasOwn-guarded; Bearer ADMIN_PASSWORD constant-time. **admin.html:** Quill reply-editor fixes (insertCanned used .value no-op → Quill API; typing guards used activeElement.id==='replyText' always-false → window.quill.hasFocus(); double Quill init via render token; both send buttons disable together; attach button note-only since /reply drops attachments; SSE reconnect after permanent close; jsArg() for onclick injection). **Docs:** Study_Plan.md corrected (versions/download-routes/5-flavor build); ERRATA_2026-07-12.md written for the PDFs (file:// stale, 1.3.0/22 not current, self-updater direct-only). 3 website commits UNPUSHED (backend, admin, 1.3.3 APK), all authored Akhil, no Claude trailer per [[no-claude-coauthor]]. apps/ is NOT a git repo (Drive-synced on disk). PENDING USER: push website; upload 1.3.3 AAB to Play; Register&Activate 1.3.3; other-store uploads; on-device test (esp. preset persistence + update banner).

- **FEEDBACK UI REDESIGN + FOCUSED AUDIT → 1.3.4 (code 28) + 1.3.5 (code 29) 2026-07-12:** user reported feedback "dead button" (was just the <10-char validation with no hint) + wanted a redesign. **1.3.4:** App.tsx FeedbackCard rebuilt — two buttons: "Submit feedback" opens a slide-up bottom-sheet tray with the form (+ live char hint "N more characters" so the send button is never a mystery), "Contact us" opens gamepad.space/contact.html via AndroidBridge.openUrl (openExternal helper). Then a **4-agent focused bug-hunt** (scoped to today's changes only, each agent self-verifies, NO separate verify wave — all 4 finished, no session-limit deaths) found 8 bugs all in today's code; fixed the real ones → **1.3.5:** (a) feedback tray now always opens fresh via openTray (no stale-field/duplicate-send/reset-timer races); (b) UpdateChecker visibilitychange re-check guarded by busyRef so it doesn't blank an available banner or clobber a live download; (c) **MainActivity shouldOverrideUrlLoading now matches PARSED host** (u.scheme=="https" && u.host=="appassets.androidplatform.net") — the earlier startsWith() check I wrote allowed lookalikes like appassets.androidplatform.net@evil.com into the privileged WebView; (d) server.js register-activate versionCode validated with /^\d+$/ (parseInt is lenient: "1.3.5"→1 passed the old Number.isInteger guard → would store code 1 and break the manifest); (e) admin.html reply editor focused once per (ticket+mode) via _lastFocusedKey (background refresh no longer steals caret from search box); (f) sendNote double-Enter guard. tsc+gradle+node --check all pass. Browser preview of the tray was blocked by a transient classifier outage but bundle strings verified present in the APK. 3 website commits UNPUSHED (1.3.4 APK, focused-audit backend/admin, 1.3.5 APK); the earlier bug-hunt backend/admin/session-cookie commits are PUSHED+LIVE. Store listing URLs live (uptodown/amazon/play). PENDING USER: push the 3 commits; upload 1.3.5 AAB to Play + Register&Activate; other-store uploads; on-device test.

- **STANDARD-PAD MODULARITY + EDITOR UX PASS 2026-07-13 (DEBUG builds only, iterated live on device via adb install -r):** user-driven session. (1) **"ltrt" compound widget REMOVED entirely** (types.ts union, gameplay render, editor previews/footprints/DEFAULT_R/SELF_MAPPED_TYPES) — LT/RT are always two independent "trigger" widgets; `splitLegacyLtrt()` in App.tsx migrates saved pads on load (same footprint: pill w=r, h=r*5.85, centres x±(gap/2+pillW/2)), verified via injected legacy pad. (2) Standard layout buttons ENLARGED (LB/RB 52→65, ABXY 140→165, DPAD 138→165, view/home/menu+stickmode 40→50, hybrid stick 120→145; LT/RT separate triggers at x=1000/1180 w=160 h=480... later w/h 210/480→160 to kill overlap). (3) Editor toolbar (back/gear/+) pinned to top edge (top-8→top-1); **two-step selection**: tapping a widget only selects (drag immediately), settings panel opens via the gear TOGGLE button in the toolbar (showSettings state) so the panel never hides the widget being placed. (4) Add-Widget menu de-lagged: single scroll container (was nested outer+inner), header pinned, backdrop-blur removed, touch-pan-y. (5) **framer gotcha ×2 fixed:** Tailwind -translate-x-1/2 centering is clobbered by motion's inline transform during springs (panel slid sideways) → centre with a flex wrapper; AnimatePresence direct children MUST be motion components (plain-div wrapper broke exit tracking) → all 3 editor popups restructured. (6) Back button skips the "Unsaved Changes" confirm when nothing changed (initialButtonsJson ref snapshot); empty pads (0 widgets) auto-delete on editor close (both onSave and onCancel paths in App.tsx). versionCode bumped 32/1.3.8 in build.gradle.kts (NOT store-built; only direct-flavor DEBUG installed on phone). **BUILD PIPELINE GOTCHA: the Android app bundles a manual COPY of the web UI — `npm run build` in apps/controller-ui then Copy-Item dist/index.html → apps/android-client/app/src/main/assets/dist/index.html, else the APK ships a stale bundle** (bit us once this session; explains "fix didn't arrive on phone"). PREVIEW GOTCHA: the in-app browser preview tab is document.hidden → rAF never fires → ALL framer animations freeze (mount stuck at opacity 0, AnimatePresence exits never unmount, screenshots time out) — verify state via DOM/localStorage, not animation completion; phone-side behavior is fine. adb screenshots: `adb shell screencap -p /sdcard/x.png` + pull works great for device UI review. PENDING: on-device user validation of 1.3.8 editor UX; store release of 1.3.7/1.3.8 still unshipped (stores serve 1.3.1).

- **PC SERVER 1.1.17 — WINDOW-CLOSE LEAK FIX + PILLOW-12 BUILD FIX 2026-07-13 (built + installed on THIS PC, verified):** user reported "new server not working, error msg on open" = the benign single-instance popup *"Gamepad Server is already running…"*, but the ROOT CAUSE was a real leak: **closing the GUI (✕) did NOT terminate the process.** `on_close` set stop_event + `root.destroy()` and returned from mainloop, but the asyncio WS/AOA bridges keep non-daemon library threads alive → the process lingered invisibly in the background; every "close" left a ghost, and the next launch hit the single-instance mutex → duplicates piled up. FIX (server.py `on_close` ~L1814): after `root.destroy()`, run `atexit._run_exitfuncs()` (preserves adb kill-server + timeEndPeriod cleanup) then **`os._exit(0)`** to hard-terminate. Verified via WM_CLOSE PostMessage: process now fully exits (both PyInstaller procs gone in <12s); before, they stayed forever. **SECOND bug found while rebuilding — environment drift:** the current PC env has **Pillow 12.2.0**, which added a new `PIL.ImageText` module that `ImageDraw` imports unconditionally, and ImageText imports `ImageFont` at top. The spec's size-trim **excluded `PIL.ImageFont` + dropped `_imagingft`** (old comment: "QR is just rectangles, no font-text needed") → the rebuilt exe crashed at startup with `ImportError: cannot import name 'ImageFont' from 'PIL'` (traceback through qrcode.make_image → ImageDraw → ImageText). FIX (GamepadServer.spec): removed `'PIL.ImageFont'` from excludes and trimmed `_pil_dead` to only `('_avif','_webp','_imagingcms')` (keep `_imagingft`/`_imagingmath`/`_imagingmorph`). Confirmed the real import chain by tracing `qrcode.make_image` in-env (loads PIL.ImageFont+ImageText+_imagingft). Rebuilt 25.9MB exe (was 24.8 broken / vs old 42.8 GRX build), replaced installed `F:\keys\Gamepad Server\GamepadServer.exe` (old backed up as `.bak-1.1.16`), launched clean (QR + pairing code 777a51b3, no error log). Versions bumped 1.1.16→**1.1.17** in all 3 touchpoints (server.py APP_VERSION, .iss AppVersion + VersionInfoVersion). **GOTCHA for any future PC-server rebuild: don't re-exclude PIL.ImageFont/_imagingft while Pillow≥12; PyInstaller 6.20 + Python 3.14.3 env.** INSTALLER BUILT + STAGED + COMMITTED (same session): Inno Setup compiled `GamepadServer-Setup.exe` 26.7MB v1.1.17.0 (SHA-256 `9FB2393BEC8F639F73336F575C801F03F77338ED2EAB40C12CBAA6343B69303E`); staged to website/backend/downloads/ as both `GamepadServer-Setup-1.1.17.exe` + canonical `GamepadServer-Setup.exe`; frontend/index.html .dl-trust SHA-256 + size (~40→~27 MB) updated; committed to website repo (a9436d0, author Akhil, NO Claude trailer, **UNPUSHED** — 1 commit ahead of origin/main). frontend/dist is gitignored (Vercel rebuilds from source). **REMAINING (user): git push website (deploys), admin portal Register & Activate PC 1.1.17 (flips /api/version — until then in-app updater still advertises 1.1.16). Local PC already running the fixed exe.** Inno build cmd: `& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" GamepadServer.iss` from installer/. PS here-string with a quote in the commit msg mangles `git commit -m` → use `git commit -F <file>`.

- **WIRELESS-CONNECT DIAGNOSIS + TRANSPORT-COORDINATOR FIX → APK 1.3.9/code 33 2026-07-13 (installed on phone, on-device test PENDING):** user's PC (Lenovo LOQ, Wi-Fi 192.168.1.34) fails wireless: phone scans QR, shows "connecting to 192.168.1.34", then "connection failed"; works on other PCs. EXHAUSTIVE PC-side diagnosis proved the **PC server is healthy**: get_lan_ip correctly picks 192.168.1.34 (score 100 vs VirtualBox 192.168.56.1 score 20; _lan_score at server.py:418), binds UDP 0.0.0.0:7777, and a crafted 20-byte frame (`<Q H B B B B B B I`, token=int("777a51b3",16)) sent to it got an "ACK" reply — receive+auth+ACK all work. Firewall: unrestricted inbound allow rule "RemoteGamepad Server UDP" UDP 7777-7786 Any/Any, Wi-Fi profile=Private, no block rules, no 3rd-party firewall (only McAfee WebAdvisor browser add-on). BUT a live diagnostic listener on 7777 (mimicking the server) caught **ZERO packets** when the phone attempted to connect → phone→PC UDP is dropped ON THE NETWORK before reaching this PC (likely router AP/client-isolation, or phone Wi-Fi/USB routing conflict when USB is plugged). The app/server CANNOT fix a router isolation drop. THEN user revealed the real workflow pain: connects with phone USB-plugged, sets Wired mode to "USB Tethering" to avoid USB-debugging taking over, but (Bug A) **the Wired-mode selection resets itself** and (Bug B) **PC shows a device that connects then disappears**. ROOT CAUSE (both): a QR scan (Dialogs.tsx QRScanOverlay) and manual connect (App.tsx handleManualConnect) did `localStorage.setItem("gp_wired_pref","auto")` — silently clobbering the user's explicit Wired-mode choice — because the transport coordinator (App.tsx reconcile every 1500ms) would otherwise stopEngine() a forced-pref native link. FIX (1.3.9): replaced both clobbers with a transient `window.__wirelessIntentAt = Date.now()`; the coordinator now, at the top of reconcile, protects a live/recent wireless link (`wirelessLive = nativeLink && ctype==="wireless"`, OR intent < 8s) by closing the WS + returning early WITHOUT touching gp_wired_pref → the user's Wired-mode selection persists AND a fresh Wi-Fi connect isn't killed / doesn't open a 2nd-pad WS. Coordinator modes unchanged otherwise (usbdebug→WS+stopEngine, tether→native-only, auto→native wins). Built+installed direct-debug 1.3.9 on phone. PENDING user on-device test: (1) Wired mode stays on "USB Tethering" after a QR scan; (2) no connect-then-disappear over USB tethering; (3) for pure Wi-Fi, the packet-drop is a ROUTER issue — try disabling AP/client isolation on the router, or just use USB tethering (now that its mode selection sticks). Diagnostic method that worked: craft a valid UDP frame + listen for ACK to prove server health; run a logging listener on 7777 to prove whether phone packets arrive. GOTCHA: `import server` HANGS (module-level blocks) so can't probe get_lan_ip via import — read the logic instead; frozen exe binds fine but source `python server.py` stalls before bind (admin/firewall path differs).

- **STANDARD PAD = USER'S "MY CUSTOM PAD" + EDITOR RENAME → APK 1.3.10/code 34 2026-07-13 (installed on phone):** user wanted the built-in Standard Controller replaced with a specific custom pad they designed on-device. Pulled it off the phone via **WebView remote-debugging (CDP)**: `adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>` (pid from `adb shell pidof com.gamepad.client`), GET http://localhost:9222/json for the page's webSocketDebuggerUrl, then a Python `websockets` client → Runtime.evaluate `localStorage.getItem('custom_pads')`. Two pads found: "My Custom Pad" (12 widgets, anchored refined-standard: LB/RB, ABXY diamond, DPAD, view/home/menu, L/R stickmode, Hybrid stick, independent LT/RT trigger pills w/ rxFactor) and "My Custom Pad (Copy)" (13-widget experimental — individual X/A/Y/B on left, single LS, digital triggers). Used **"My Custom Pad"** — its exact coords/sizes/anchors transcribed into `getTrueStandardLayout()` (App.tsx; the `w()` helper extended to carry rxFactor). Since the controller builds activePad from `makeDefaultPad("standard")` → getTrueStandardLayout when playing the preset, this makes the Standard Controller BE the user's design (preset render path honors anchors via offsetCenter/offsetRight loop). ALSO added an editor **rename ✎ pill** (top-left of CustomPadEditor): padName state, tap→inline input (maxLength 24), included in onSave `{...pad, name}` + the back-button dirty-check. Both verified in vite preview (standard card shows LB/RB/ABXY/dpad/view-home-menu/L/R/H/LT/RT; rename pill opens prefilled input). Built+installed direct-debug 1.3.10. CDP-read method is the clean way to extract on-device localStorage (leveldb parse avoided). PENDING: user on-device confirm the standard pad matches their design + rename persists.

- **NEW THEME + SPLASH + PLAY EDGE-TO-EDGE → 1.3.16/code 40 2026-07-14 (directDebug BUILD SUCCESSFUL; store/release build + on-device test PENDING):** user wanted a professional non-neon theme + kill the ~1s black launch flash + address Play Console "next release" items on the store 1.3.0/code 24. (1) **"Graphite Steel" theme** replaces the neon cyan `#00d4ff` + purple `#7c3aed`/`#a855f7` palette: charcoal bg `#0B0E14`, surface `#151A23`, steel-blue primary `#4F86C6`, slate accent `#64748B`. Rewrote styles/theme.css (all CSS vars, both :root + .dark) + fonts.css (Oxanium→**Space Grotesk** display + Inter body; h1-h3 now Space Grotesk). Remapped ~133 hardcoded colors across App.tsx/Dialogs/Widgets/GamepadWidgets/CustomPadEditor via **literal (non-regex, non-anchored) PowerShell String.Replace with a scratchpad backup first** (respects the App.tsx corruption lesson) — mappings incl `#00d4ff`→`#4F86C6`, `rgba(0,212,255`→`rgba(79,134,198`, purple→`#64748B`, navy bgs→graphite, `'Oxanium'`→`'Space Grotesk'`. Also harmonized the decorative "racing red" steering/rumble sliders (`accent-red-600`/`bg-red-900`→`accent-[#4F86C6]`/`bg-white/10`) + the "Left/right device rotation" label (text-red-500→text-primary/80); left SEMANTIC reds (errors, delete confirms, disconnected dot, OFFLINE-PREVIEW banner) alone. Vite build clean; verified in browser preview (dashboard + System tab, 0 console errors, no neon). (2) **Native Android 12 SplashScreen** (androidx.core:core-splashscreen:1.0.1) kills the black cold-start window: new res/values/themes.xml `Theme.App.Splash` (parent Theme.SplashScreen, windowSplashScreenBackground=@color/splash_bg `#070910` = the JS splash bg exactly, animated icon=@drawable/ic_launcher_foreground, postSplashScreenTheme=Theme.AppCompat.NoActionBar); manifest application theme → Theme.App.Splash; MainActivity `installSplashScreen()` BEFORE super.onCreate + `setKeepOnScreenCondition{!uiReady}` + 220ms fade-out exit listener + 3s failsafe; new `onUiReady()` bridge flips uiReady; App.tsx splash effect calls `AndroidBridge.onUiReady()` after 2×rAF so the native splash dissolves seamlessly into the identical JS splash (same #070910 + logo) — no black, no blank frame. Also smoothed the JS splash (entrance rise/scale via splashIn, breathing `.splash-glow` keyframe, easing 600/620ms). decorView black→`#070910`. (3) **Play "deprecated edge-to-edge APIs"**: dropped the deprecated `View.SYSTEM_UI_FLAG_*` bitmask in hideSystemUI → WindowInsetsControllerCompat.hide only (edge-to-edge already set via setDecorFitsSystemWindows(false); insets already dispatched to `--android-safe-*` CSS vars). The other 2 Play items were already handled (16KB align = CameraX 1.4.0; large-screen = intentional portrait lock). versionCode 39→40, versionName 1.3.16. Rebuilt controller-ui dist + copied dist/index.html → android-client assets/dist/ (the build gotcha). directDebug APK built OK (2m49s, app-direct-debug.apk 9.2MB) validating all native changes compile. NOT store-built. PENDING user: release/AAB build of all flavors, on-device test (splash smoothness + theme + pairing/input unaffected), then store uploads + Register&Activate. See [[feedback-gyro-indicator-design]] (gyro indicator spec unchanged).

**Key honest verdict:** sub-1ms is real ONLY for the encrypted *transport hop* (~0.5-0.9ms p50 on a reworked AOA USB path, IF the phone enumerates USB high-speed — unverified). True finger→game is ~12-22ms (sensor ~200Hz cap + the game's own XInput poll dominate). Market "encrypted input", NOT "sub-1ms latency". The current app has NO real encryption — just a replayable cleartext 32-bit authToken (the #1 fix).

**DONE + verified this session (all on disk under F:\hlooo, NOT in git — apps aren't tracked):**
- `apps/docs/GRX_PROTOCOL.md` — byte-for-byte wire contract (X25519 per-connection + HKDF-SHA256 → per-direction AES-128-GCM, 96-bit counter nonce never-reset, tag-first replay window, transcript+PSK-bound handshake → kills MITM/downgrade).
- `apps/pc-server/grx_crypto.py` — AEAD + replay; **6/6 self-tests pass** (`python grx_crypto.py`).
- `apps/pc-server/grx_session.py` — handshake state machine + framing; **full client↔server self-test passes**.
- `apps/android-client/.../GrxCrypto.kt` — mirrors the Python byte-for-byte (Tink X25519+HKDF, javax AES-GCM); has `pskFromPairingKey()` + `GRX_LTID` matching the server. **NOT build-verified yet.**
- `apps/android-client/.../GrxClient.kt` — transport-agnostic handshake session-manager (start/onServerMessage/seal). Reduces the device-session to minimal glue. build.gradle += tink-android 1.13.0.
- `apps/docs/GRX_INTEGRATION.md` — exact server.py hook + Android wiring plan + device-test checklist.
- **server.py WIRED (UDP/WiFi+USB-tether path):** `run_udp_loop` now routes GRX handshake frames + decrypts 41B input behind `GRX_REQUIRED=False` (legacy cleartext preserved). PSK = `_grx_psk_from_key(key)` = HKDF-SHA256(hexBytes(pairing key), salt="", info="grx psk v1", 32); `GRX_LTID=b"gamepados-grx-v1"`. No QR/pairing change. `requirements.txt`+=cryptography; spec hiddenimports += grx_crypto/grx_session/cryptography; build.gradle += tink-android. server.py parses OK. (WS/AOA transports + s2c rumble encryption still cleartext — v2.)

**ALSO IN FLIGHT (same 10:30 build/test session) — 60/120fps UI smoothness (separate from GRX):** Root causes found + FIXED in code: (1) Android never requested high refresh → app ran at 60Hz on 120Hz phones. FIXED: MainActivity.onCreate now picks `display.supportedModes.maxByOrNull{refreshRate}` → sets `preferredDisplayModeId` + `preferredRefreshRate` (the enabling fix). (2) The render-loop bar glide used per-frame smoothing (`dispBx += (target-disp)*0.45` EVERY frame → 2× faster at 120Hz = inconsistent feel). FIXED in App.tsx: frame-rate-INDEPENDENT `alpha = 1-exp(-36*dt)` (rate 36 reproduces the old 60Hz feel, identical at any refresh). Audit confirmed this was the ONLY frame-based loop (other rAF = single-shot CSS-transition triggers, already time-based; 29 CSS transitions inherently refresh-agnostic). DEFERRED bonus: input send is fixed 60Hz (setInterval(16), App.tsx:962) — tie to GRX packet-rate work. Pending: build + device-test feel on a 120Hz phone.

**Rollback preserved:** `F:\hlooo\releases-archive\v1.1.9-android\` (APK+src) and `v1.1.5-pc\` (exe+src) — the known-good builds before GRX.

**Kotlin glue ALREADY ADDED (dormant):** MainActivity.kt has `GRX_ENABLED=false` flag, `grx: GrxClient?`, JNI decls `nativeGrxSendRaw`/`nativeSetGrxReady`, up-call targets `grxSeal()`/`onGrxControl()`, and grx init in connectToPC — all gated off so the working app is untouched (external fns resolve lazily, never called while disabled). The **complete C++ JNI bridge is ready-to-paste** in GRX_ANDROID_WIRING.md (Option A): globals+JNI_OnLoad+grxEnv()+nativeGrxSendRaw/SetGrxReady+the hot-path seal hook (replaces send at gamepad-engine.cpp:269/273, guarded by grxReady atomic, default false)+the RX control-frame route.

**C++ ADDITIVE BRIDGE NOW APPLIED in gamepad-engine.cpp** (after packetCount global ~line 82, and before initNetworkNative ~line 550): g_vm/g_activity/g_mid_seal/g_mid_ctrl/grxReady globals, grxEnv() helper, JNI_OnLoad (caches g_vm), and the two JNI down-calls nativeGrxSendRaw + nativeSetGrxReady. All dormant (grxReady=false, unread by existing code) → file still compiles, behavior unchanged. NOT compile-verified (no NDK here).

**ALL WIRING NOW APPLIED (both sides) — only a flag-flip + build remain.** gamepad-engine.cpp: (a) initNetworkNative caches g_activity + grxSeal/onGrxControl method ids; (b) hot-path send uses outBuf/outLen = sealed 41B when grxReady else raw 20B (UDP path; AOA still raw); (c) RX loop routes 0xE1/E2/E3 frames up via onGrxControl (rxBuffer enlarged 32→128 for the 65B SERVER_HELLO). MainActivity.kt: grx init in connectToPC + grx?.start() after initNetworkNative (null-safe). EVERYTHING is dormant: GRX_ENABLED=false (grx stays null → start/seal no-op) and grxReady=false (hot-path sends legacy) → working app byte-identical until flipped.

**NEXT (device, ~3 steps):** (1) flip `GRX_ENABLED=true` in MainActivity.kt; (2) build APK (compile-verify the NDK C++ + Kotlin + Tink 1.13.0 — NOT verified here, no NDK); (3) install + pair + confirm encrypted input drives the pad + check RTT for no regression; (4) flip server `GRX_REQUIRED=True` to retire the cleartext path, bump versions. If the native build errors, the bridge code is also documented in GRX_ANDROID_WIRING.md.

(superseded plan below)
**NEXT — ONE device-gated step left, precisely specced in `apps/docs/GRX_ANDROID_WIRING.md`.** Architecture fact (from reading the code): the **C++ engine owns the UDP socket** (create gamepad-engine.cpp:468, connect :407, send :269/:273, authToken from initNetworkNative→expectedHash; key from MainActivity connectToPC :327/:345). So GRX needs a JNI bridge, not pure Kotlin glue. Recommended **Option A** (reuse tested Kotlin crypto): add C++ `grxReady` atomic (default false→legacy path safe), JNI down-call `nativeGrxSendRaw`, hot-path hook to up-call Kotlin `grxSeal(20B)→41B`, RX hook routing 0xE1/E2/E3 control frames up to Kotlin `onGrxControl`. Kotlin: build GrxClient(pskFromPairingKey(key), GRX_LTID){nativeGrxSendRaw(it)}, start() on connect, set grxReady when established. Option B = native mbedTLS crypto in C++ (preserves hot path, more work) — only if JNI up-call cost matters. Then build APK + device-test + flip GRX_REQUIRED. Other Track-1 quick wins still pending: fix 2ms send quantizer, WiFi-gated server prediction, sustainedPerformanceMode + gyro thread, disable USB selective suspend.

Related: [[project-downloads-feedback]], [[project-realtime-latency-stack]], [[feedback-no-auto-push]], [[feedback-git-author-vercel]].

**GYRO-FREEZE-ON-BUTTON-PRESS ROOT CAUSE + FIX (2026-07-20):** user bug "gyro lags/freezes when tapping buttons, smooth when tapping fast". MEASURED via CDP probe injected into live WebView (762 taps): Chromium renderer scheduler `touchstart` use-case DEFERS DOM TIMER queues ~90-160ms on EVERY discrete tap (399 holes/2min, starting ~10-20ms before JS pointerdown fires); rAF unaffected (p95 8.4ms @120Hz); ZERO long tasks → NOT React, NOT GPU, NOT CPU-clocks. Old useGyro 8ms setInterval data loop = the entire gyro→packet path → froze steering ~100ms/tap (C++ TX just re-sends stale currentPayload). FIX (App.tsx useGyro): data read moved from setInterval(8) onto the rAF tick (`pumpGyro()` called at top of the render loop) — immune to timer deferral, same ~8.3ms cadence on 120Hz. Also React.memo'd Btn/Dpad/RightStick/TriggerPill in Widgets.tsx (good hygiene, was NOT the cause). Definitive future fix = "Phase 3a" native gyro injection: Kotlin onSensorChanged→JNI atomics→C++ TX applies gyro to stick bytes at send time (design agreed, not yet built). ⚠️ **MainActivity.kt temporarily enables setWebContentsDebuggingEnabled UNCONDITIONALLY (TODO at ~line 372) for CDP diagnosis — MUST revert to `if (BuildConfig.DEBUG)` before ANY store/website artifact build.** CDP loop: adb forward tcp:9223 localabstract:webview_devtools_remote_<pid>, scratchpad cdp.mjs (node 24 global WebSocket); app must be FOREGROUND or evaluate hangs (Android 16 freezes cached procs). Phone build = 1.3.22/46 direct-release with fix installed 2026-07-20 ~19:45; store artifacts in releases/store/1.3.22 do NOT have any of this.
**2026-07-20 later:** user CONFIRMED gyro-freeze fix works on device ("Ok now"). MainActivity CDP-unconditional flag REVERTED in source to `if (BuildConfig.DEBUG)` (phone still runs the CDP-enabled build — reinstalling any future build removes it). Phase 3a native gyro injection still pending user go-ahead. Store 1.3.22 artifacts still WITHOUT the gyro fix — rebuild all 6 variants before upload when user says.
Session handoff for the gyro-fix session: `F:\hlooo\docs\handoffs\SESSION_HANDOFF_2026-07-20B-gyrofix.md` (supplements the same-day latency handoff). CDP driver persisted to `F:\hlooo\tools\webview-cdp.mjs`.
